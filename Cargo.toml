[package]
name = "frontier"
version = "0.1.0"
authors = ["Parity Team <admin@parity.io>"]
build = "build.rs"

[dependencies]
error-chain = "0.12"
node-cli = { path = "cli" }
futures = "0.1"
ctrlc = { version = "3.0", features = ["termination"] }

[build-dependencies]
vergen = "2"

[workspace]
members = [
	"cli",
	"executor",
	"primitives",
	"runtime",
]
exclude = [
	"runtime/wasm",
]

[profile.release]
panic = "unwind"
